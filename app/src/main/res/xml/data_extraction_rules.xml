<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ backup rules. Both cloud-backup and device-transfer are fully
  excluded: the Room database is plaintext, so allowing either would place
  unencrypted medication history outside the device's trust boundary.
  Cross-device restore is served by the app's own end-to-end encrypted sync.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
